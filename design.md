main.js

frankenstrument.js
class (inst1={
  title=[
    "Saxo",
    "phone"
  ]
  fadeOutTime, //Time when the attack starts to fade out
  fadeInTime, 
  notes=[
    { example:
      note:"a4",
      src:"path/to/audio"
    }
  ]
})

public Methods:
playNote("a4")
playMidi(midiData)
stop()
get name()